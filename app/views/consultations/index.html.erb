
<h1 style="text-align:center;">Consultations</h1>
<p style="text-align:center;"></p>

<p>
<table class="table table-hover table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Posts</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @consultations.each do |consultation| %>
      <tr>
        <td><%= consultation.name %></td>
        <td><%= consultation.posts.count %></td>
        <td><%= link_to 'View', consultation %></td>
        <% if current_user.is_doctor? %>
          <td><%= link_to 'Edit', edit_consultation_path(consultation) %></td>
          <td><%= link_to 'Destroy', consultation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

    <%= link_to 'New Consultation', new_consultation_path %>
</p>